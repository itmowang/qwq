# Desktop service endpoints are loaded from resources/portmax-endpoints.json.
# The packaged installer copies that non-secret file into its resources directory.
# To override an installed client, create %APPDATA%\Portmax Desktop\portmax-endpoints.json
# with `mastraServerUrl` and `portmaxApiUrl` HTTP(S) URLs.
